# QA

`web-qa-sheet.xlsx` — 웹(`frontend/`) QA 시트. 5탭이다.

| 탭 | 내용 |
|---|---|
| 대시보드 & 엄수 규칙 | 라운드·기준 문서·집계 수식 · 명세 18장 인수 테스트 판정 · 이 시트가 못 잡는 것 |
| TC 시나리오 | 케이스별 시나리오·기대 결과·판정. `라운드` 열로 1차(수정 전)와 2차(수정 후)를 나눈다 |
| 요청사항(REQ) | Fail 로 등록한 이슈. 심각도·재현·증거·해결 방향과 실제 수정 커밋 |
| 공통 TC 크로스 | 전 화면에 걸리는 공통 기준의 라운드별 결과 |
| 기능 기준표 | 기능 ID ↔ 기대 동작. 근거는 `feature-spec.md` 조항 |

`evidence/` — REQ 가 인용하는 화면 증거.

## 어떻게 만들었나

배포본(`https://farmfi.co.kr`)을 Playwright(Chromium)로 직접 조작하고 API 를 직접 호출했다.
읽기만 하지 않고 청약·증빙 제출·마일스톤 승인 같은 쓰기 경로까지 실행했으며, 끝난 뒤
`/api/demo/reset` 으로 시드를 복구했다. 기대 결과는 `feature-spec.md` 에서 가져왔고, 명세가
정하지 않은 화면은 케이스를 만들지 않았다.

수정 후 재검증(2차)은 같은 코드의 로컬 dev 에서 했다. 1차 판정은 덮어쓰지 않고 `TC-X`·`TC-Y`
코드로 따로 남긴다 — 무엇이 깨져 있었는지가 기록에서 사라지면 같은 자리를 다시 깨뜨린다.

## 다음 라운드에 할 것

- 배포 후 프로덕션에서 2차 TC 를 한 번 더 돌린다. 지금 2차는 로컬 dev 기준이다.
- 모바일 신분증 발급 요청 구간(`/verify/mobile-id`)은 OACX 설정이 있는 프로덕션에서만 확인된다.
- 명세 18장의 금칙어 기준을 다시 본다. 화면 라벨에 "토큰"이 들어간 자리가 생겼다.
