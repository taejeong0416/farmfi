# 개발 로그 (Dev Log)

> 무엇을 했고 왜 그렇게 결정했는지를 남기는 **작업 기록**. **최신을 맨 위에** 쓰고, 끝난 항목은 자유롭게 압축·정리한다 (전체 이력은 git).
> 날짜 형식: `## YYYY-MM-DD — 작성자`

---

## 2026-08-20 — 우민성

### 온체인 mint가 실제로 성사됐다 — 막고 있던 것 두 개

발행 파이프라인을 만든 뒤 계속 `PENDING`으로만 남았다. 원인이 둘이었고, 하나만 고쳤으면 여전히 실패했다.

- **기본 Amoy RPC가 죽어 있다** — `rpc-amoy.polygon.technology`가 연결 자체를 거부한다(HTTP 000). `polygon-amoy-bor-rpc.publicnode.com`·`polygon-amoy.drpc.org`는 정상이다. `ONCHAIN_RPC_URL`을 publicnode로 바꿨다.
- **서버 지갑에 `MINTER_ROLE`이 없었다** ← 진짜 원인. `FarmToken` 생성자가 `DEFAULT_ADMIN_ROLE`과 `REGISTRAR_ROLE`만 부여하고 `MINTER_ROLE`은 아무에게도 주지 않는다. 배포 이후 지금까지 **아무도 mint를 할 수 없는 상태**였다. 배포자가 관리자 권한을 갖고 있어 자기 자신에게 부여했다(tx `0x5f91f037…`). **새 체인에 배포하면 이 단계를 반드시 다시 해야 한다.**

**확인**: 데모 1~3단계 실행 → 토스가 발급한 가상계좌 → 입금 확정 → 청약 → 수탁 지갑 → 온체인 mint 3건 전부 `CONFIRMED`.
`0xf5585229…`(300구좌) · `0x3109748c…`(200) · `0xb57344d7…`(420).
대사 결과 **DB 920구좌 ↔ 체인 920구좌, 불일치 0건**.

### OmniOne Chain 전환은 포트가 막혀 보류

Oracle 인스턴스(168.138.36.235)는 살아 있고 OpenDID 서비스가 전부 응답한다(8090·8091·8092·8094·8099 → HTTP 200). 그런데 **Besu RPC 포트가 외부에 열려 있지 않다** — 8545·8546·8645·9545·22001 전부 무응답. OpenDID 전용이라 노출할 이유가 없었을 것이다.

전환하려면 순서가 이렇다: ① Oracle 보안목록 + iptables에서 8545 개방 → ② `FarmToken`·`Escrow`·`Dividend` 재배포 → ③ `ONCHAIN_*` 전환(`ONCHAIN_GAS_ZERO=true`) → ④ **`MINTER_ROLE` 부여**. ④를 빼면 위와 같은 자리에서 다시 막힌다.

그때까지 프로덕션은 Amoy로 둔다. Vercel `ONCHAIN_*`가 26일 전부터 등록돼 있었는데 Sensitive라 값을 읽을 수 없어, 검증된 Amoy 설정으로 덮어썼다.

### 수탁 지갑과 보유 구좌 발행을 붙였다 (P0-8 · Phase P1~P4)

명세 0장은 "Chain Relay·Outbox·eventId 멱등성이 v2.0에 이미 있으므로 파이프라인 신설은 없다"고 적었지만, 실제로는 셋 다 없었다. `confirmBankDeposit`은 팀원이 이번 주에 만들어 붙일 자리는 생겼고, 그 옆에 발행을 이어 붙였다.

- **아웃박스를 따로 만들지 않았다** — `HoldingIssuance` 행이 곧 아웃박스다. 입금 확인과 **같은 트랜잭션**에서 PENDING으로 생기므로 "입금은 확정됐는데 발행 의도가 사라진" 상태가 없고, 프로세스가 중간에 죽어도 남은 행을 이어서 처리한다. 일반 Outbox 테이블을 두면 명세 10장의 데이터 모델과 두 벌이 된다.
- **멱등은 DB가 건다** — `eventId = deposit:<거래번호>:mint`에 unique. 애플리케이션 검사만으로는 동시 웹훅 두 개를 못 막는다. 웹훅 3회를 쏴서 발행 행 1개·입금 이벤트 1개를 확인했다.
- **체인 전송 전에 청약 결과를 본다** — 명세는 "confirmDeposit 성공 후 mint"라고만 적었는데, 우리 파이프라인에는 청약 반영(`settleInvestment`)이 한 단계 더 있고 그게 실패할 수 있다(잔여 물량 부족·한도 초과). 그대로 두면 **환불 대상에 구좌를 발행한다.** 실제로 테스트에서 그 상태가 나왔다. `DEPOSIT_FAILED`·`CANCELLED`면 발행을 `CANCELLED`로 닫고, `COMPLETED`가 아니면 기다린다.
- **키는 봉해서 넣는다** — `CUSTODY_MASTER_KEY`(32바이트)로 AES-256-GCM. DB에는 `local:v1:<iv>:<tag>:<ct>` 형태의 `keyRef`만 든다. 마스터 키가 없으면 지갑을 아예 만들지 않는다 — 평문 저장으로 물러설 여지를 남기지 않았다. 운영은 `kms:` 접두로 갈아 끼운다.
- **재전송 방지선을 하나 더 뒀다** — 해시를 받자마자 DB에 적고 그 다음에 영수증을 기다린다. 영수증 대기 중 죽으면 재시도가 같은 mint를 두 번 보낼 수 있는데, 해시가 이미 있으면 전송하지 않고 확정만 찍는다.
- **`HoldingLedger`를 새로 배포하지 않았다** — 이미 배포된 `FarmToken`이 그 역할을 한다. `decimals() == 0`이라 1구좌 = 정수 1로 맞고, `_update`의 화이트리스트 게이트가 발행(`from == 0`)을 예외로 둬서 신원 등록 전에도 발행된다. 2차 이전은 양쪽 다 `registerIdentity`가 필요해 별도 단위로 남겼다.
- **`FundCustodyAdapter`는 Mock이고 그렇게 말한다** — 화면에 "투자금 보관 · 분리보관 · 출시 시 신탁 적용"을 띄운다. 문구는 어댑터가 주므로 신탁사가 붙으면 화면 코드를 고치지 않아도 표시가 따라 바뀐다. "에스크로"라고 부르지 않는다.
- **투자자 화면에는 아무것도 늘지 않았다** — 지갑·주소·토큰·발행 상태는 전부 admin 창구(`/api/admin/issuances`)로만 나간다. 원장 구조가 바뀐 것이지 화면 언어가 바뀐 게 아니다.

**확인**: 임시 프로젝트·투자자를 만들어 웹훅 3회 → 신청 `COMPLETED`, 판매량 5구좌 반영, 입금 이벤트 1건, 발행 행 1건, 수탁 지갑 생성 후 복호하니 주소 일치, DB에 평문 키 없음. 청약 실패 건은 발행이 `CANCELLED`로 닫혔다. 테스트로 만든 데이터는 전부 지웠다.

**확인하지 못한 것**: 실제 체인 전송. 이 환경에서 Amoy RPC(`rpc-amoy.polygon.technology`)에 도달하지 못한다(연결 자체가 안 된다). 그래서 발행 행이 `PENDING`으로 남고 재시도가 예약되는 것까지만 봤다 — 실패 처리 경로는 확인됐고, mint가 체인에서 성사되는지는 **RPC가 닿는 곳에서 다시 봐야 한다.** calldata selector가 `0x40c10f19`(`mint(address,uint256)`)인 것까지는 맞췄다.

### 증빙 없이 자금이 나가는 구간을 막았다 (P0-9)

v2.1 명세가 P0로 올린 "운영자 증빙 제출(O-11) · 관리자 승인(A-08) → 승인된 단계만 집행"에서, 화면 두 개와 API 세 개는 이미 있었는데 **게이트가 없었다.** 실제로 뚫려 있던 경로:

```
pending ─(admin이 /verify 호출, AI 통과)→ verified ─(/complete)→ 자금 집행
```

증빙 제출은 한 번도 거치지 않는다. 운영 DB에서 MF01 1단계가 `verified` · 증빙 0건 · 트랜치 1,540만원 집행 대기 상태로 실제로 남아 있었다. 옛 경로가 만든 상태다.

- **집행 자격 판정을 `lib/milestone-gate.ts` 한 곳으로 모았다** — `canRelease`는 상태값만 보지 않는다. 증빙이 존재하는지(`evidenceSubmittedAt`), 그 증빙에 판정이 있었는지(`reviewedAt` 또는 `aiVerificationResult`)까지 본다. 상태값 하나만 믿으면 수동 조작·마이그레이션 잔재로 `verified`가 된 행이 그대로 통과한다.
- **`/verify`도 증빙을 요구한다** — 검증은 제출된 증빙을 판정하는 일이다. 증빙이 없으면 판정할 대상이 없고, 여기서 통과시키면 `/complete`가 곧바로 자금을 내보낸다.
- **`requiredSignals`가 빈 배열이면 거부한다** — `Object.values({}).every(...)`가 공허참으로 `true`가 되어 신호 0개짜리 단계가 무조건 통과하고 있었다.
- **`canSubmitEvidence`에 `in_progress`를 넣었다** — `/complete`가 다음 단계를 `in_progress`로 여는데 그 상태가 제출 가능 목록에 없었다. 2단계부터는 운영자가 증빙을 낼 수 없었다는 뜻이다.
- **`canReview`에 `verified`를 넣었다** — 증빙 없이 `verified`가 된 옛 행을 보완 요청으로 되돌릴 길이 없으면 그 단계는 집행도 회수도 못 하는 채로 남는다. 승인(`approve`)은 증빙을 요구하고, 되돌리는 방향인 보완 요청(`revise`)은 요구하지 않는다.
- **데모도 같은 문으로 들어간다** — `demo/step`이 `/verify` 전에 `/evidence`를 먼저 호출한다. 시연이 "증빙 제출 → AI 검증 → 트랜치 집행"이라는 실제 순서를 그대로 보여주게 됐다. seq 2는 IoT만 보는 단계라 첨부 문서가 없어 가동 현장 사진 한 장으로 형식을 맞춘다.
- **관리자 콘솔이 이유를 먼저 보여준다** — `MilestoneVerifyPanel`이 증빙 제출 여부를 표시하고, 없으면 검증 버튼을 잠근다. 눌러서 400을 보게 하지 않는다.
- **상태 라벨 복사본을 없앴다** — `components/screens/api.ts`가 같은 표를 따로 들고 있어 상태 흐름을 고치면 한쪽만 바뀌었다. `milestone-gate.ts`를 원본으로 재수출한다. 그 김에 `in_progress` 라벨을 "검증중"에서 "진행중"으로 고쳤다 — 직전 단계 집행 직후 다음 단계가 들어오는 자리라 검증과 무관하다.

**확인**: 로컬 dev + 운영 DB로 실행. ① `verified`·증빙 0건 단계 집행 → 400 ② `pending` 단계 검증 → 400 ③ 증빙 없는 승인 → 400 ④ 증빙 제출 → `evidence_submitted` ⑤ 승인 → `verified`+`reviewedAt` ⑥ 그 행에 `canRelease` → `{ok:true}`. 마지막 집행은 실행하지 않았다 — 온체인이 켜져 있어 데모 데이터에 실제 Amoy 트랜잭션이 나간다. 테스트로 바꾼 행과 감사 로그는 원상복구했다.

### 스키마가 DB에 반영돼 있지 않았다 — 로그인 500

`schema.prisma`에는 병합돼 있는데 운영 DB에는 없던 것: `User.ciHash` · `User.identityProvider`, 그리고 `SensorThreshold` · `Device` · `DeviceCommand` · `NotificationPref` · `StockAdjustment` 5개 테이블. User 조회가 전부 `P2022 ColumnNotFound`로 죽어 **로그인이 500이었다.**

- `prisma db push`는 팀 테이블 40개를 드롭하려 해서 쓰지 않는다. `frontend/prisma/sql/001_phase_u_additive.sql`에 `ADD COLUMN IF NOT EXISTS` · `CREATE TABLE IF NOT EXISTS`만으로 적고 한 트랜잭션으로 적용했다. 재실행해도 안전하다.
- **RLS는 켜지 않았다** — 이 스키마의 기존 33개 테이블이 전부 RLS 미사용이다. DB는 브라우저 anon 키가 아니라 Next 서버의 특권 커넥션으로만 닿고 Prisma 롤은 RLS를 우회한다. 5개만 켜면 일관성만 깨진다. 접근 통제는 라우트의 `requireRole`·세션 게이트가 맡는다.
- 적용 후 스키마와 DB가 완전히 일치하고, 로그인·`/api/me/notification-settings`·`/api/devices`·`/api/products` 전부 200.

---

## 2026-08-20 — 박태정

### 생육 레시피 — 검증 가능한 목표 학습으로

레시피 층이 "환경↔수율에서 최적 생육조건을 찾는다"고 주장하는데 그 주장을 확인할 수단이 없었다. 확인 수단부터 만들고, 그 위에서 좌표계·불확실성·목적함수를 차례로 고쳤다.

- **합성 반응면이 안장점이었다**: temp×CO₂ 교차항 계수 0.01이 오목성 상한 2√(a_t·a_c)=9.8e-4의 10배였다. 진짜 최적점은 `OPT`가 아니라 표본 상자 모서리(16℃·400ppm, 수율 31.9 vs OPT의 5)에 있었고, 화면 숫자가 그럴듯했던 건 농학 클램프가 막아준 덕이다. **클램프가 알고리즘을 검증하는 게 아니라 대체하고 있었다.** 테스트 베드를 `growth-recipe-synth.ts`로 분리하고 스펙마다 촐레스키로 오목성을 자체 검사한다. 오목하면 최적점이 항상 `center`이므로 회수 테스트가 성립한다 — 상호작용은 최적점을 옮기지 않고 표면 모양만 바꾼다.
- **품종 좌표를 GDD로 두려다 접었다**: (T−base)/(mid−base) 비를 다시 범위폭으로 나누면 base가 약분되어 (T−mid)/half와 같아진다. 남는 차이는 사이트 편차를 ℃로 볼 것이냐 범위 상대로 볼 것이냐뿐인데, 그 편차의 원인은 센서 위치·공조 배치라 어느 쪽도 결정적이지 않다. GDD가 실제로 필요한 곳은 품종 이전이 아니라 사이클 안의 시간 적분이다. 좌표는 z=(x−mid)/half 하나로 두고, 이 정의가 농학 사전의 (mu, sd)와 같은 값이 되게 해 좌표계와 사전이 어긋나지 않게 했다. 부수적으로 원시 단위의 co2² 열(1e6 규모)이 사라져 설계행렬 조건수 문제도 같이 없어졌다.
- **이전 강도를 곱셈으로만 두면 상한이 안 생긴다**: 정밀도에 w를 곱하면 w가 "얼마나 빨리"만 정하고 "어디까지"를 못 정한다. 상추 데이터가 넉넉하면 w=0.11인 토마토도 문헌을 밀어냈다(실측: 이전 비중 89%). 품종 불일치 자체를 분산으로 세워 δ(w)=PRIOR_SD·√(1/w−1)를 더하니, 원 데이터가 무한히 쌓여도 이전 비중이 정확히 w로 수렴한다. "이전계수 0.21 = 최대 21%까지만 민다"로 읽히는 게 의도한 의미였다.
- **사전 정밀도가 표본 수의 함수였다**: `seTheta = sd·√(20/n)`이라 잡음이 5배 다른 두 데이터셋이 같은 가중치를 받았다. 정작 델타법 SE는 `inv3`까지 90줄이 짜인 채 아무도 부르지 않았다(1D 정점이 자주 null이라 갈아탄 흔적). 되돌리는 대신 부트스트랩으로 갈았다 — 관측을 복원추출해 표면을 다시 적합하고 최적점 분포의 산포를 본다. 탐색 상자는 원표본으로 고정한다(재표본마다 다시 잡으면 산포에 상자가 흔들린 몫이 섞인다).
- **실험 제안이 죽어 있었다**: 판정식 `√(20/(n+20)) > 0.5`는 곧 n<60이라, 표본 120개인 데모에서 언제나 빈 배열이었고 화면 가드가 length로 걸려 패널이 한 번도 안 떴다. 사후 분산이 실제 값이 된 지금은 잡음이 큰 데이터에서 제안이 나오고, 잘 관측된 데이터에서는 "추가 실험 불요"가 문장으로 뜬다.
- **목적함수에 비용이 없었다**: 레시피가 수율만 최대화하니 비싼 목표를 정하고 스케줄 층이 그걸 싸게 달성하는 구조였다. `수익 = 가격×수율(x) − 운전비용(x)` 하나로 합쳤다. 수율은 학습된 표면에서, 비용은 요금·물리에서 온다. 엽채류 기준 목표 DLI가 15.95→13.13으로 내려오고 수율은 5.19→5.05kg/㎡로 줄지만 운전비가 더 크게 줄어 수익이 하루 979원 는다. 비용이 붙는 요인은 DLI·CO₂·온도 셋뿐이고 습도·EC·pH는 이 모델에서 비용이 없다는 걸 표에 밝힌다 — 제습 전력과 양액 원가를 셀 근거가 없어 지어내지 않았다.
- **비용 없는 EC가 따라 움직인다**: 상호작용으로 DLI와 엮여 있어서다. 요인을 따로 봤으면 놓쳤을 이동이라 노트에 싣는다.
- **갭 분석이 부분합과 전체를 어긋나게 내놓고 있었다**: 요인별 상방을 "이 요인만 옮겼을 때의 차이"로 재면 상호작용 때문에 합이 안 맞는데 화면은 둘을 나란히 놓는다. 섀플리 분해로 바꿔 항등식이 되게 했고, `max(0, …)` 클램프도 없앴다 — 한 요인만 옮기면 손해인 경우가 실제로 있다.
- **진단 셋을 표면에 붙였다**: 헤시안이 음정부호가 아니면 안장점으로 표시하고 갭 분석이 권고를 내지 않는다. 최적점이 탐색범위 끝에 붙으면 경계해로 표시한다("여기가 최적"이 아니라 "여기까지만 봤다"). 표본이 파라미터의 2배에 못 미치면 R²는 0이 아니라 null이다 — 0은 설명력 없음이라 판정 불가와 뜻이 다르다.
- **회귀 테스트 30건 추가** — 전체 80건 통과. 남은 것: 관측이 여전히 합성 데이터고(`HarvestRecord`↔`IotData` 연결과 EC 필드 부재는 별건), `growthRecipeDemo` 한 번이 585ms라 `DemoCache`에 얹을 자리가 있다.

### Phase O · 투자금 납입을 가상계좌 경로로

동의 → 건별 가상계좌 발급 → 은행 입금 확인 → 청약 반영. 지급사는 `lib/payment.ts`의 어댑터 뒤에 있고 지금은 Mock이 은행 역할을 한다.

- **`POST /api/investments/[id]/deposit`를 지웠다**: 즉시 확정 경로를 남겨두면 가상계좌를 우회하는 문이 하나 열려 있는 셈이다. `settleInvestment`는 입금이 확인된(`DEPOSIT_CONFIRMED`) 신청에서만 돈다.
- **입금 확인을 웹훅에만 걸지 않았다**: 로컬과 데모에는 웹훅을 보내 줄 은행이 없다. `GET .../deposit-status`가 어댑터에 입금을 물어보고 웹훅과 같은 `confirmBankDeposit`을 탄다. 거래번호가 계좌당 고정이라 몇 번을 조회해도 `providerTransactionId` unique가 한 번만 통과시킨다.
- **금액 불일치·기한 만료를 신청 상태로 만들지 않았다**: 상태값을 9개에서 더 늘리면 라벨·필터·화면 분기가 전부 갈라진다. 신청은 `AWAITING_DEPOSIT`에 그대로 두고 `failureCode`로 네 예외를 구분한다. 돈이 확정되지 않았다는 사실과도 맞는다.
- **Mock 예금주 조회는 이름을 지어내지 않는다**: 지급사가 없으니 계좌 주인을 알 방법이 없다. 어댑터는 조회 성공만 알리고, 본인 명의 판정은 호출부가 본인확인 실명과 대조해서 한다. 실 API가 붙으면 이 대조는 그대로 두고 어댑터 응답만 바뀐다.
- **신청을 취소하면 가상계좌도 닫는다**: 닫힌 계좌로 입금이 들어오면 확정하지 않고 관리자 검토 큐로 보낸다(`GET /api/admin/deposits/review-queue`). 금액 불일치·기한 초과 입금도 같은 큐다.

### 잔여 기능 계획 — 기능명세서와 코드 대조

명세서에 있는데 코드에 없는 것을 API 라우트 64개·Prisma 모델 30개·화면 데이터 경로와 대조해 뽑고, Phase O~W로 세웠다.

- **별도 문서를 만들지 않았다**: 진행 상황의 정본이 `build-plan.md`의 체크박스인데 계획 파일이 둘이면 어디를 보고 이어받는지가 갈린다. 화면 이식(A~M)·디자인 최종본(N) 뒤에 기능(O~W)을 이어 붙였다.
- **체크된 화면인데 서버가 없는 것들**: C-I03 계좌 확인, 운영자 방문·교육·계약(전부 `OperatorApplication` PATCH 하나에 얹혀 있다), B-09 픽업 바코드(`patchSubscription`으로 대신하는 중). 화면 이식 단계에서는 정상이지만 명세 기준으로는 미구현이라 잔여로 잡았다.
- **붙어 있지 않은 라이브러리**: `growth-recipe.ts`·`control-loop.ts`는 구현돼 있는데 어느 라우트도 import하지 않는다. W1·V4는 새로 짜는 게 아니라 연결하는 일이다.
- **화면 ID가 명세와 Figma에서 서로 다른 것을 가리킨다**: 명세 O-13은 레시피, Figma O-13은 정산 내역이다. A-10/A-11도 어긋난다. ID로 부르면 엉뚱한 화면에 기능을 붙이게 되므로 대조표를 계획에 넣고, 기능은 이름으로 찾기로 했다.
- **범위 밖을 다시 그었다**: 수탁 지갑과 `FundCustodyAdapter`를 "화면에 안 보이므로 제외"로 뒀었는데, 명세 18장의 MVP 완료 기준이 입금→기록→발행을 요구한다. 범위 안(Phase P)으로 옮기고, 범위 밖은 컨트랙트 코드 수정과 외부 사업자 실계약으로 좁혔다.

### `.fig`가 잠정본임을 문서에 반영

지금까지 이식한 화면 98개는 전부 잠정 `.fig`를 보고 그린 것이다. 문서는 이걸 확정 디자인처럼 적고 있었다 — `build-plan.md`가 "Figma 확정 화면", "Figma가 절대 기준"이라고 썼고 Phase A~M이 전부 체크돼 있어서, 읽는 사람은 화면이 최종이라고 받아들이게 된다.

- **체크는 지우지 않는다**: 잠정본 기준의 이식·검증은 실제로 끝났다. 대신 체크의 뜻을 문서 상단에서 "잠정본 기준 완료"로 못 박고, 최종 디자인 일치 여부는 새 Phase N의 체크가 말하게 했다. 끝난 일을 안 끝난 것으로 돌리면 무엇이 남았는지가 더 흐려진다.
- **Phase N을 미체크로 세웠다**: 최종 `.fig` 교체·덤프 재생성 → 화면 목록 대조 → 생김새 대조 → 문구 → 검증. "체크 안 된 첫 항목부터"라는 규칙이 가리킬 자리가 생긴다. 파일이 오기 전까지는 대기 상태라고 적어뒀다.
- **파생 문서에도 단서를 달았다**: `figma-route-map.md`의 라우트 표와 `figma-color-map.md`의 색 집계는 잠정본에서 뽑은 값이라 최종본에서 화면 ID가 바뀌거나 새 색이 들어올 수 있다.

### 웹 프론트 — Figma 62개 화면 이식 + 신규 백엔드

Phase A~K를 끝냈다. 화면과 그 화면이 돌아가는 데 필요한 모델·API를 짝으로 만들었다. 판단이 갈렸던 지점만 남긴다.

- **Tailwind 토큰은 덧붙이는 쪽으로**: `text-base`·`rounded-lg` 같은 기본 스케일을 Figma 값으로 덮어쓰면 아직 안 옮긴 화면의 글자 크기가 같이 흔들린다. 대신 `text-11`~`text-28`, `rounded-6`~`rounded-14`처럼 실측값을 그대로 쓰는 키를 새로 얹었다. Figma 덤프의 숫자가 클래스명에 그대로 보여 대조하기도 쉽다.
- **헤더·푸터는 layout에서 한 번만**: Figma는 모든 화면에 같은 상단바를 그렸다. 화면마다 헤더를 넣는 대신 `layout.tsx`의 `SiteHeader`·`AppFooter`로 한 곳에 뒀고, 로그인 역할에 따라 진입 링크만 바뀐다.
- **C-04는 역할을 바꾸지 않는다**: 이용 목적 선택 화면이 계정 역할을 바꾸는 것처럼 보이지만, Figma 문구가 "로그인한 계정으로 투자·정기구독·운영 서비스를 이용할 수 있어요"다. 한 계정이 셋 다 쓴다는 뜻이라 이 화면은 라우팅만 한다. `PATCH /api/auth/me`는 건드리지 않았다.
- **`Investment`는 청약을 감싼다**: 다단계 신청 상태를 새 파이프라인으로 만들지 않고, 마지막 납입 단계에서 기존 `executeSubscription`을 호출하고 결과만 기록한다(`lib/investment.ts`). 잔액·재고·연간한도 검증이 한 곳에 남는다.
- **증빙 게이트는 기존 상태 어휘에 얹었다**: 계획서의 대문자 상태(`EVIDENCE_SUBMITTED` 등)를 그대로 쓰면 소문자 상태를 쓰는 기존 verify·complete·timeout 라우트와 앱이 전부 깨진다. `evidence_submitted`·`revision_required` 두 개만 추가하고 나머지는 기존 이름을 살렸다. 집행 게이트는 이미 `complete`가 `verified`만 통과시키므로 그대로 둔다.
- **운영자 7단계는 모델 하나에**: 방문 예약·교육 이수·공간 확정·계약 서명마다 모델을 세우는 대신 `OperatorApplication`에 필드를 붙이고 `PATCH ?step=`으로 밀었다. 앞 단계가 안 끝나면 다음 단계를 거부한다.
- **정기구독 회차를 미리 만든다**: 매장에서 보여줄 확인번호가 회차마다 필요해서, 신청 시점에 앞으로 4회차를 `PickupOrder`로 만들어 둔다. 확인번호는 구독 id와 날짜에서 계산해 재발급해도 같은 값이 나온다.
- **구매자용 재고 조회는 따로**: `/api/inventory`는 operator 전용이라 구매자가 부를 수 없다. 수치를 그대로 내리는 대신 "고를 수 있는지"만 담은 `/api/subscriptions/catalog`를 새로 뒀다.
- **A-15는 감사 로그에서 뽑는다**: AML 화면의 데이터 원천이 따로 없다. 이상거래를 지어내는 대신 감사 로그에서 기한 초과·검증 반려·하루 다건 청약을 골라 보여준다. 연간한도 초과와 본인확인 미완료는 API에서 이미 거부되므로 목록에 오르지 않는다고 화면에 적었다.
- **금지 용어는 화면·응답·감사 로그까지**: `.tsx` 전체에서 0건. `Escrow`·`TokenHolding`·`Dividend` 모델명과 기존 라우트의 설계 주석은 남는다 — 모델명을 바꾸려면 데이터 마이그레이션이 따라야 해서 v2.1 모델 정리로 넘겼다. 범위는 `build-plan.md` 맨 끝에 적어뒀다.
- **없는 화면은 리다이렉트로**: 지갑 등록·재연결 URL은 만들지 않고 `middleware.ts`에서 `/verify/account`로 보낸다. `/verify-identity`도 새 흐름으로 넘긴다.

검증: `npx tsc --noEmit` 통과, `next dev`에서 라우트 50개 + 동적 라우트 11개 전부 200(비로그인 `/mypage`는 307), 렌더 오류 0건.

---

## 2026-08-20 — 박태정

### 운영자 앱 프론트 — Figma 36개 화면 이식

Phase L·M을 끝냈다. 백엔드는 손대지 않고 화면만 옮겼다. 판단이 갈렸던 지점만 남긴다.

- **EC 대신 pH**: Figma 베드 카드·센서 타일에 EC 칸이 있는데 `IotData`가 수집하는 값은 `phLevel`이다. EC 라벨을 달면 없는 값을 있다고 말하게 되므로 pH로 표기했다. EC를 실제로 수집하기 시작하면 `SENSOR_META`에 키를 추가하면 된다.
- **센서는 지점 단위**: Figma는 베드 4개가 각각 다른 판독을 가진 것처럼 그렸지만 센서는 프로젝트 단위로 저장된다. 베드마다 값을 흩뿌리는 대신 08 화면 상단에 "환경 센서는 매장 단위로 수집됩니다"라고 적고 같은 값을 보게 뒀다.
- **쓰기 경로가 없는 동작**: 관찰 기록·일정 등록·재고 조정·품목 등록·설비 제어·임계값 저장·알림 설정·리포트 내보내기는 API가 없다. 버튼을 죽이는 대신 화면 안에서 반영하고, 팝업이 "이 기기에만 반영됩니다 · 서버 저장은 ○○ API 연결 후"라고 밝힌다. 흐름은 데모할 수 있고 거짓말은 하지 않는 선.
- **21 제어 실패의 조건**: 실패 화면을 만들려고 실패를 지어내지 않았다. 센서 응답이 `stale`인 지점은 설비가 실제로 바뀌었는지 확인할 길이 없으므로, 그 경우에만 제어 실패로 처리한다.
- **일정은 재고에서 만든다**: 일정 API가 없어 06 재배 일정은 `Inventory`의 `plantedAt`·`expectedHarvestAt`에서 파종·수확 일정을 만든다. 별도 모델을 세우기 전까지는 이게 실제 데이터다.
- **픽셀 에셋은 남긴다**: 픽셀아트 화면 8개는 새 화면이 대체해 지웠지만 스프라이트 자체는 Figma가 비워둔 자리(`이미지 준비 중`·`Bed thumb (교체)`·`SceneCard`·`MiniPlants`)에 그대로 들어간다. `components.tsx`는 그 시각 요소만 남기고 껍데기·탭·상태 표시는 `ui/`로 넘겼다.
- **기기 크롬은 안 옮긴다**: Figma의 Status Bar·Home Indicator 프레임은 목업이다. 실제 기기에서는 OS가 그리고 `SafeAreaView`가 자리를 잡는다. 색 치환표의 판단 보류였던 `#070428`은 이 결정으로 함께 사라졌다.

**미결**: `src/components/app-tabs.web.tsx`가 존재하지 않는 `/explore` 라우트를 가리켜 `npx tsc --noEmit`이 실패한다. Expo 템플릿 잔재로 어디서도 import되지 않는다. 앱의 타입 검증을 깨끗하게 하려면 지워야 한다.

---

## 2026-08-19 — 박태정

### 명세 v2.1 통합 — 문서를 개발 착수 가능한 상태로

기획팀에서 받은 v2.0 기능명세서와 v2.1 개정 패치를 하나로 합쳤다. 패치는 바뀐 섹션만 담은 형태라 그대로 두면 v2.0을 계속 옆에 놓고 봐야 했다.

v2.1의 세 결정: 수탁 지갑 방식 토큰 발행(화면 비노출), 마일스톤 증빙 제출·승인 P0 승격, 자금 보관을 신탁으로 표기하고 "에스크로" 용어 삭제.

- **웹 명세 통합**: 패치가 지정한 교체분(0.2·0.3·1.1·1.2·3.3·3.4·9.1·9.3~9.6·10·15·16.2)을 제자리에 반영. 패치에 없지만 모순이 남는 곳도 함께 정리 — 17장의 "토큰으로 전환되지 않으며", I-07의 보유 표기, C-I03·C-I04의 지갑 잔재. P0인데 사양이 없던 O-11·A-08 절을 신규 작성(내용은 패치 3-C·9.6 범위 안에서만).
- **기획 리뷰 반영 3건**: 0.4에 위험 고지 우선 규칙(순화한 용어가 원금 보장으로 읽히는 것을 막고, 손실 가능성 문구는 순화 대상에서 제외). A-08에 조건 항목별 판정과 이해상충 회피(집행 요청자 ≠ 승인자, 관리자 1인 환경에서는 이의제기 대상 자동 표시). 16.3 신탁 게이트의 통과 조건 4개 명시.
- **되돌린 것**: 체인 보유 원장을 서버 DB의 사본으로 규정하는 문장을 넣었다가 뺐다. 0.2는 명시적 교체 섹션이고 "보유 원장을 체인에 둔다"가 결정이다. 3.5의 "서버 상태가 단일 진실 공급원"은 상태값(`DRAFT`/`CHAIN_PENDING`) 규칙이라 층이 다르다. 다만 **체인 원장이 DB 대비 무엇을 하는지는 여전히 문서에 없다** — 기획팀 확인 대기.
- **앱 명세 재작성**: 기존 문서는 템플릿 슬롯 형식이라 화면 ID·라우트·API·상태·데이터 모델이 없었다. 웹 명세와 같은 형식으로 다시 쓰고 화면 ID(M-01~M-14)와 Expo Router 경로를 붙였다. 웹이 앱에 요구하는데 앱 문서에 없던 3개를 추가 — 보증서 VC 확인(O-08), 픽업 바코드 스캔(B-09), 증빙 사진 제출(O-11).
- **유저플로우 2개 삭제**: `user-flow.md`는 구 계보(지갑 바인딩·에스크로 락업·보유 토큰·배당)라 새 명세와 충돌했고, 구매자·운영자 신청 플로우가 통째로 빠져 있었다. `app-user-flow.md`도 함께 정리. 화면 위치는 명세의 화면 ID·라우트 표가 대신한다.

**미결**: 체인 보유 원장의 존재 이유(위), `units` 노출 범위, 레포 코드와 명세의 간극(현재 코드는 Polygon Amoy + `Escrow.sol` 계보라 BESU·Chain Relay·수탁 지갑이 없음).

---

## 2026-08-17 — 박태정

### 웹 명세 미구현분 — 백엔드층을 먼저 끝낸다
`feature-spec.md`와 코드를 대조해 남은 개발을 갈랐다. 기준은 **프론트 UI 시안 없이 확정할 수 있는가**다. 화면 5개(목록 검색·필터, 리스크 고지 섹션, 청약 취소·연결 계좌·배당 캘린더, 운영자 증빙 제출, 알림 설정)는 시안을 받은 뒤로 미루고, 스키마 변경이 걸린 다섯 항목을 먼저 끝냈다. 마이그레이션이 발표 직전에 몰리는 것을 피하려는 순서다. (UI 대기 목록은 바탕화면 `farmfi-web-ui-pending.md`)

- **스키마 5모델**: `AuditLog`(FK 없이 값만 — 대상 행이 지워져도 기록은 남는다)·`SettlementRule`(프로젝트별 override)·`Appeal`/`AppealComment`·`Payout`. 겸해서 `Project.operatorId`와 `ProjectPartner.userId` — 운영자 몫·임대료를 누구에게 지급할지 알 수 없었다. 세션 pooler(5432) additive push.
- **감사 추적**(명세 2.5): `recordAudit`은 기록 실패가 청약·트랜치 집행을 롤백시키지 않게 예외를 삼킨다(트랜잭션 클라이언트를 넘긴 경로는 전파). `GET /api/audit-logs`에 기간·프로젝트·사용자·이벤트 필터 + 커서 페이징 + CSV(UTF-8 BOM — Excel 한글). 청약·검증·반려·집행·기한초과·배당·환불·알림에 배선.
- **정산 규칙**(2.1·2.1.1): `waterfall.ts` 하드코딩 상수를 `DEFAULT_*` 기본값으로 내리고 `resolveSettlementRule`이 프로젝트 규칙을 읽는다(규칙 없으면 기존 수치 그대로). 우선배분·전환은 기간 매출과 `breakEvenRevenue` 비교로 적자/흑자 배분율을 고른다. `FeePoolResult`에 `rule·band·investorShare`를 실어 화면이 판정 근거를 보여줄 수 있게 했다.
- **이의제기**(1.3·1.3.1): 보류·반려 건에만 접수, 마일스톤당 미결 1건. `review → escalate → approve/reject` 전이를 조건부 `updateMany`로 원자화. 인정되면 `manual_review` 마일스톤을 `in_progress`·retry 0으로 되돌려 재검증 경로를 연다(기한초과 `failed`는 환불 절차라 손대지 않음). 외부 전문가는 계정 역할이 없어 admin이 `asRole=auditor`로 대신 기록한다.
- **프로젝트 승인·중지**(2.4.1): 사유 필수. `resume` 복귀 상태는 저장값이 아니라 현재 사실관계(모집 마감·완판·마일스톤 완료)에서 유도한다 — 저장한 이전 상태와 실제 진행도가 어긋나는 경우를 없애려고. `completed`·`failed`는 이 경로로 되돌리지 않는다(정산·환불 절차와 어긋남).
- **지급 원장**(2.2·2.2.1): 실 이체는 범위 밖이라 예정/완료/실패 상태와 증빙만 다룬다 — 잔액은 건드리지 않는다(즉시 지급은 `dividends/distribute`가 담당). 배당(구좌 비례)·임대료(파트너 월 고정)·운영자 정산(매출 − 이용료 − 임대료) 3항목. 유니크 제약으로 재호출이 안전하고, `format=csv`가 명세의 '지급 파일'이다.

### 스모크에서 잡힌 것
`tsc`만으로는 안 보이는 것들이 실호출에서 나왔다. dev 서버 + admin Bearer로 신규 라우트 전부 왕복(상태를 원위치로 되돌리는 시나리오만) 후 흔적 정리.
- **이의제기가 통과한 마일스톤에도 열렸다**: `retryCount>0`만 봐서, 1회 실패 후 재검증을 통과한 `verified` 건이 접수 대상에 들어갔다. `verified`·`completed` 제외로 수정.
- **운영자 정산 라인이 안 생겼다**: 코드가 아니라 데이터 — 시드가 돌던 시점엔 `Project.operatorId`가 없어 기존 행이 전부 null이었다. 시드 정의와 같은 값(정하은)으로 백필하고 재확인 → `매출 1,400,000 − 이용료 200,000 − 임대료 500,000 = 700,000` 생성.
- **구간 전환 확인**: `breakEvenRevenue=1,400,000`·적자 0.8 규칙에서 매출 100만 → `band=deficit`, 배당 262,857(= 풀 328,571 × 0.8). 규칙 삭제 후 기본값 0.6 복귀도 확인.
- 가드 확인: 사유 없는 중지·최종판정, 1 초과 비율, 잘못된 상태 전이, 중복 이의제기, 완료 건 재지급 — 전부 4xx.

---

## 2026-08-03 — 박태정

### 생육 모니터링 — 광량을 일적산으로, 생장을 환경의 함수로
"생장 모니터링"인데 화면이 전부 환경 센서값이었고, 광량 판정이 최적화와 정면으로 충돌하고 있었다. 실데이터가 없다는 제약이 오히려 순서를 정했다 — 탐지기를 더 만들기 전에 **시드가 탐지기가 잡을 현상을 담고 있는지**부터 봐야 했다.

- **시드 시나리오화**(`iot-seed.ts`): 기존 시드는 2% 무작위 단발 스파이크뿐이라 CUSUM은 탐지할 대상이 없고 DLI는 매일 같은 값이었다. 세 고장을 시간축에 심음 — 12~18일차 냉방 저하(하루 +0.6℃, CUSUM만 잡음) / 30일차 펌프 막힘(pH 4.2, Z-score+게이트) / 45일차~ LED 열화(−28%, DLI만 잡음). `growthRate`를 날짜 함수에서 **환경 함수**로 교체: GDD로 진행하고 DLI 부족분만큼 지연.
- **광량 판정 전환**: 최적화(`dliSchedule`)가 광주기를 심야로 옮기고 시간 압축 시 PPFD를 올리는데, 모니터링은 순간 조도 상한만 봤다 → 심야 점등이 오탐이 되고 LED 열화는 안 걸림. 광량을 순간값 판정에서 빼고 **생육일 일적산(DLI) 달성률**로 판정. 생육일 경계는 자정이 아니라 **가장 어두운 시각** — 점등 블록이 어디로 옮겨가든 한 버킷에 담기게.
- **2단 밴드**: `iot-health.HEALTHY_RANGES`와 `crop-profiles.healthyRanges`가 서로 다른 기준을 쓰고 있었다. crop-profiles를 단일 출처로 두고 `faultRanges()`가 여유폭을 더해 고장 게이트를 파생. 최적대 이탈=주의(제어루프가 되돌림) / 게이트 이탈=심각(사람이 감).
- **GDD 수확 예측**: 적산은 광 제약을 반영한 유효 GDD로 쌓는다(온도만 세면 광 부족한 날도 만근으로 잡혀 진행률이 실제 생장을 앞지름).
- **알림 배선**: `iot/generate`가 Z-score 이상일 때만 Notification을 만들고 있었다. 화면과 같은 `analyzeGrowthMonitoring`을 태워 고장 게이트·CUSUM 드리프트·DLI 미달까지 통지(6시간 쿨다운).

### CUSUM 드리프트 탐지 — 회귀 테스트가 드러낸 오탐 3건
회귀 테스트를 쓰자 "무고장 시드에서 전 센서 드리프트 탐지"가 나왔다. 원인이 셋 겹쳐 있었음:
- **lag 미전달**: `cusumDrift`의 기본 lag=24는 시간당 표본 전제인데 모니터링이 30분 데이터에 그대로 호출 → 12시간 차분이 되어 일주기를 역위상으로 증폭.
- **차분 자체가 원인**: lag를 고쳐도 오탐. 차분은 저주파 흔들림을 lag 길이의 동일부호 런으로 바꾸는데(측정 acf(24)=−0.57) 그게 정확히 관리도가 잡도록 설계된 패턴. → **일중앙값**에 차분 없이 관리도를 건다(평균은 단발 스파이크에 오염됨).
- **관리한계 고정**: H=5의 ARL₀가 465표본이라 2,880표본 창에선 오경보가 확정. Siegmund 근사로도 부족(i.i.d. 전제 불일치) → 관리이탈점(|z|>3)을 걸러낸 **관리상태 표본 부트스트랩**으로 한계를 잡음.
- **잘린 부분일**: 오경보가 전부 마지막 버킷에서 나왔다. DLI가 쓰던 `complete` 기준을 관리도에도 적용.
- **결과**: 무고장 오경보 60일 5%·30일 0%·7일 0%, 냉방저하는 12일차로 특정(통계량 88.6 vs 한계 ~10). 회귀 테스트 16건 추가 — 전체 50건 통과.

### 화면
- **웹**: 생장 섹션 신설(DLI 막대 + 목표선, GDD 진행률 + 생장곡선 + D-day), 센서 차트 2단 밴드, 이벤트 타임라인에 DLI 미달 구간.
- **앱**: DLI 막대·수확 예측 패널, 드리프트 시점 세로선, X축을 인덱스 균등에서 **시간 비례**로, 30일 2,880점 다운샘플(이상치 보존).

---

## 2026-06 ~ 2026-08-14 — 이전 작업 요약

세부 이력은 git history에 있다. 결정과 재발 가능한 함정만 남긴다.

**문서 체계 (08-14)** — `docs/`가 11개까지 늘어 기준이 흐려져 기획 단계 문서와 구현 서술 문서를 정리하고 3개로 줄였다. 기준은 "개발할 때 무엇을 만들지 보는 문서인가". 구현을 서술하는 문서(`api-spec.md` 등)는 코드가 앞서가는 순간 틀린 문서가 된다 — 구현의 현재 상태는 코드가 답한다.

**데모 오케스트레이션 (07-21 ~ 07-23)** — `demo/reset`·`demo/step` 8스텝 복원. 시드 로직을 `seed-scenario.ts`로 추출해 CLI 시드와 런타임 리셋이 같은 코드를 쓴다. 라이브 e2e에서 통합버그 3건: `demo/step`이 프로젝트를 `status="funding"`으로 찾다가 완납 후 못 찾던 것(`tokenSymbol` 고정으로 해결), `detect-anomaly`가 `uptimeRate`를 반환한 적이 없어 IoT 마일스톤이 항상 실패하던 것, 배당 분배가 인터랙티브 트랜잭션 기본 5s를 넘겨 간헐 실패하던 것(15s로 확장).

**STO 재융합 (07-21)** — v18 피벗으로 제거했던 STO·온체인 레이어를 운영 인프라와 다시 합쳤다. STO 11모델 복원 + 운영 5모델 유지. Project의 STO 금융필드를 nullable로 둬 운영 전용 지점도 표현된다. 인증은 web/app 공용 JWT(bearer). 복원한 라우트 4개가 무인증이던 것을 `requireRole`로 막았다.

**v18 피벗 (07-14 ~ 07-15)** — STO·온체인 전면 제거 후 운영 인프라로 전환했다가 위 재융합으로 부분 반전됨. 제거 순서에서 배운 것: lib을 먼저 지우면 라우트가 lib을 import해 tsc가 깨진다 → 소비처를 먼저 제거해 lib을 고아화한 뒤 삭제. 인증은 SIWE(지갑)에서 이메일+비밀번호로 교체. 멀티에이전트 코드리뷰로 27건 수정.

**온체인 (06-21 ~ 06-26)** — Polygon Amoy에 FarmToken·Escrow·Dividend 배포, `verifyMilestone`·`releaseTranche` 실호출로 txHash 확보. **release는 배포당 1회성**(재실행 시 `Already released` revert) — 반복 시연은 재배포 또는 `DEMO_MODE=cached`. 체인 오류는 try/catch로 격리해 DB 데모가 안 멈춘다. `forge` fuzz/invariant 128k회로 solvency·자금 보존 검증.

**AI 검증 (06-11 ~ 06-21)** — provider 추상화(`ai-vision.ts`)로 Gemini → OpenAI → Anthropic 폴백. 현재 Gemini 무료 키로 동작. e2e에서 잡은 신호 정합 버그 2건: Gemini가 객체 배열을 반환하는데 `string[]`로 가정해 `join()`이 `"[object Object]"`가 되어 crossCheck가 항상 실패하던 것, 마일스톤 조건문이 "IoT + 영수증"을 묶어 전달해 영수증 검증기가 IoT까지 떠안던 것. **실 Gemini는 간헐적 추출 실패가 있다** — 발표 데모는 `DEMO_MODE=cached`가 안전.

**인프라 (06-07 ~ 06-19)** — 도커 미사용(Vercel + Supabase) 확정. Prisma 7 driver adapter 정착 — `new PrismaClient()` 무인자 불가, datasource url은 `prisma.config.ts`에. **DDL은 세션 pooler(5432)로만** 통한다(트랜잭션 pooler 6543은 `db push`가 멈춤), 시드 DML은 6543 정상. 무료 플랜 자동 일시정지는 대시보드 Restore로 복구. **로컬 `next build`는 한글 경로(`D:\해커톤`)에서 readlink EISDIR로 실패** — 검증은 `tsc --noEmit` + `next dev`로, 프로덕션 빌드는 Vercel.
