import { VerifyMobileIdScreen } from "@/components/screens/common/VerifyMobileIdScreen";

export const metadata = { title: "모바일 신분증 확인 | FarmFi" };

export default function VerifyMobileIdPage() {
  return <VerifyMobileIdScreen />;
}
