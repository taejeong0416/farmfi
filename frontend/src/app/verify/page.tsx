import { VerifyMethodScreen } from "@/components/screens/common/VerifyMethodScreen";

export const metadata = { title: "본인확인 방법 선택 | FarmFi" };

export default function VerifyPage() {
  return <VerifyMethodScreen />;
}
