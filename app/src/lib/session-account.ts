/**
 * 앱이 자동으로 발급받는 운영자 세션 (M-01 대체).
 *
 * 시안에서 로그인 화면이 숨겨져 있어 흐름에서 뺐다. 그런데 운영 API는 전부 JWT를
 * 요구하므로 세션 자체는 있어야 한다 — 그래서 스플래시가 이 자격으로 조용히
 * 로그인한다. 우회가 아니라 실제 세션을 발급받는다.
 *
 * **이 값을 아는 사람은 운영자로 들어온다.** 실서비스로 가려면 로그인 화면을
 * 되살리거나 기기별 프로비저닝으로 바꿔야 한다. 지금은 시연용 전제다.
 */
export const SESSION_ACCOUNT = {
  email: "operator@farmfi.test",
  password: "farmfi123",
} as const;
