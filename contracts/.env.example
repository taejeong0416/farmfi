# contracts 배포용 환경변수 샘플 — 실제 값은 .env에 넣으세요 (.env는 gitignore됨)
# forge script script/Deploy.s.sol 이 vm.envUint("PRIVATE_KEY")로 읽습니다.

PRIVATE_KEY="0x..."                              # 배포자 지갑 키 (frontend/.env와 동일 값). 테스트넷 전용
AMOY_RPC_URL="https://rpc-amoy.polygon.technology"  # 배포 시 --rpc-url 로 전달
