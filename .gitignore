
# OMC 상태 파일 (커밋 제외)
.omc/

.DS_Store

# .fig에서 뽑은 원본 이미지 (48MB). 도구로 언제든 다시 만든다 —
# python tools/figma/extract.py. 웹이 쓰는 것만 frontend/public/assets/figma에 둔다.
design/images/

# 대조 산출물 — 도구로 다시 만든다
#   python tools/figma/labels.py && node tools/figma/audit.mjs
tools/figma/labels.json
tools/figma/audit.json
tools/figma/geometry.json
tools/figma/geometry-report.json

# 파이썬 바이트코드 캐시 — 소스에서 매번 다시 만들어지고, 인터프리터 버전에
# 묶여 있어(cpython-313) 다른 버전을 쓰는 사람에게는 무의미하다.
__pycache__/
*.pyc
